# Core app
streamlit>=1.33.0

# LangChain ecosystem
langchain>=0.1.13
langchain-community>=0.0.33
sentence-transformers>=2.2.2
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.25.0

# Document loaders
PyMuPDF==1.23.7
docx2txt>=0.8

# Vector database
faiss-cpu==1.7.4.post2 ; platform_machine != 'aarch64'
# For compatibility across platforms, this version is prebuilt

# Optional: prevent tokenization-related errors
protobuf<4.0.0
